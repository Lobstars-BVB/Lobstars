---
import { Image } from "astro:assets";
import logo from "../assets/logo-full-light.png";
import team from "../assets/team.jpg";
import ContentContainer from "./ContentContainer.astro";

interface Props {
  id: string;
}

const { id } = Astro.props;
---

<section
  id={id}
  class="mx-auto flex min-h-[100svh] max-w-screen-xl flex-col items-center justify-center px-10 py-24"
>
  <a class="anchor" id={id}></a>
  <div class="flex flex-col items-center justify-center gap-10 lg:flex-row">
    <div class="lg:w-3/5">
      <Image
        src={logo}
        alt="Lobstars Ultimate Berlin"
        quality={50}
        widths={[350, 500, 700]}
        loading="eager"
        sizes="(max-width: 450px) 350, (max-width: 800) 500px, 700px"
      />
    </div>
    <div class="lg:w-3/5">
      <ContentContainer>
        <Image
          src={team}
          alt="Lobstars Ultimate Frisbee Teamphoto in Lichtenberg"
          widths={[350, 500, 800]}
          sizes="(max-width: 450px) 350, (max-width: 800) 500px, 800px"
          loading="eager"
          quality={50}
        />
      </ContentContainer>
    </div>
  </div>
  <div class="flex flex-col gap-6">
    <div class="text-center">
      <h1 class="mt-6 font-bold">Lobstars Ultimate Frisbee Club</h1>
      <h3>SV BVB 49 e.V. | Berlin Lichtenberg</h3>
    </div>
    <div class="flex justify-center gap-4">
      <a href="/#training">
        <button tabindex="-1">Train With Us</button>
      </a>
      <a href="/#contact">
        <button tabindex="-1">Contact</button>
      </a>
    </div>
  </div>
</section>
